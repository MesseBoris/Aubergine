<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
    </head>
    <body>
	<a href="{{ url("app_ticket_add") }}">Add</a>
	<table class="table">
		<tr>
            <th>poste</th>
			<th>description</th>
			<th>action</th>
        </tr>
		{% for ticket in tickets %}
            <tr>
                <td>{{ ticket.poste }}</td>
				<td>{{ ticket.description }}</td>
				<td><a href="">Commenter</a><a href="">Traiter</a><a href="">Rediriger</a><a href="">Clore</a></td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">C'est vide</td>
            </tr>
        {% endfor %}
    </body>
	</table>
</html>